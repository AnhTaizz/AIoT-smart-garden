# Development/local-demo broker. Base Compose binds it to localhost;
# compose.lan.yaml publishes it on the trusted demo LAN.
listener 1883 0.0.0.0
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
connection_messages true
